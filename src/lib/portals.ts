import * as portals from 'react-reverse-portal';

const modalPortalNode = portals.createHtmlPortalNode();
const headerPortalNode = portals.createHtmlPortalNode();

export { 
    modalPortalNode,
    headerPortalNode,
};
